---
title: 120. 三角形最小路径和
date: 2024-12-02 06:00:00 +0800
categories: [leetcode_notes, top_interview_150, 多维动态规划]
tags: [多维动态规划]
toc: true
math: true
pin: false
render_with_liquid: false
image:
  path: https://cdn.jsdelivr.net/gh/Zong-Liang/blog_images/blog/2024/daily_coding/20251121235955943.png
  lqip: data:image/webp;base64,UklGRpoAAABXRUJQVlA4WAoAAAAQAAAADwAABwAAQUxQSDIAAAARL0AmbZurmr57yyIiqE8oiG0bejIYEQTgqiDA9vqnsUSI6H+oAERp2HZ65qP/VIAWAFZQOCBCAAAA8AEAnQEqEAAIAAVAfCWkAALp8sF8rgRgAP7o9FDvMCkMde9PK7euH5M1m6VWoDXf2FkP3BqV0ZYbO6NA/VFIAAAA
---

## 题目

**描述：**

给定一个三角形 `triangle`，找出自顶向下的最小路径和。

每一步只能移动到下一行中相邻的结点上。**相邻的结点** 在这里指的是 **下标** 与上一层结点下标 **相同** 或者等于 **上一层结点下标 + 1** 的两个结点。也就是说，如果正位于当前行的下标 `i`，那么下一步可以移动到下一行的下标 `i` 或 `i + 1`。

**示例 1:**

- **输入:** `triangle = [[2],[3,4],[6,5,7],[4,1,8,3]]`
- **输出:** 11
- **解释:** 如下面简图所示：

  ```
     2
    3 4
   6 5 7
  4 1 8 3
  ```

  自顶向下的最小路径和为 11 (即, `2 + 3 + 5 + 1 = 11`)。

**示例 2:**

- **输入:** `triangle = [[-10]]`
- **输出:** -10

**提示:**

- `1 <= triangle.length <= 200`
- `triangle[0].length == 1`
- `triangle[i].length == triangle[i - 1].length + 1`
- `-10^4 <= triangle[i][j] <= 10^4`

**进阶:**

- 你可以只使用 O(n) 的额外空间（n 为三角形的总行数）来解决这个问题吗？

## 题解

这题第一眼很容易被“每步选更小的那个”带偏。三角形路径问题里，局部更小并不保证全局更优，真正稳妥的问法是：站在某个位置 `(i, j)`，从这里走到底部的最小代价是多少。问题一旦换成这个角度，状态就很清楚了。

我们定义 `dp[i][j]` 表示从 `triangle[i][j]` 出发到达底部的最小路径和。因为下一步只能去正下方或右下方，所以转移只有一条：

```text
dp[i][j] = triangle[i][j] + min(dp[i + 1][j], dp[i + 1][j + 1])
```

这个定义有个好处：边界天然在最后一行。最后一行再也走不下去，所以 `dp[n - 1][j] = triangle[n - 1][j]`，然后从倒数第二行一路往上推，直到 `dp[0][0]`，答案就出来了。

拿示例 `[[2],[3,4],[6,5,7],[4,1,8,3]]` 直观看一下。最后一行先作为初值 `[4,1,8,3]`；往上一层得到 `[7,6,10]`；再上一层得到 `[9,10]`；最后顶点 `2 + min(9,10) = 11`。这种“自底向上”的推法和转移关系完全一致，写起来也不绕。

这题还有一个常见优化：二维 `dp` 可以压成一维。因为 `dp[i][j]` 只依赖下一行的两个位置，所以每次只保留“当前可用的下一行结果”就够了。代码里的一维写法本质上做的是同一件事，只是把空间从二维降到了一维。

时间复杂度和空间复杂度可以分别来看。
- 二维 DP：时间复杂度 `O(n^2)`，空间复杂度 `O(n^2)`。
- 一维优化：时间复杂度 `O(n^2)`，空间复杂度 `O(n)`。

这题最后值得记住的不是某个公式，而是这个判断习惯：如果当前位置只依赖下一层少量状态，往往适合倒着推；再往前一步看，若依赖层数固定，就可以继续做空间压缩。

完整代码如下：
```java
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

public class Main {

    public static int minimumTotal(List<List<Integer>> triangle) {
        if (triangle == null || triangle.size() == 0) {
            return 0;
        }

        int n = triangle.size();
        int[][] dp = new int[n][n];

        for (int i = 0; i < n; i++) {
            dp[n - 1][i] = triangle.get(n - 1).get(i);
        }

        for (int i = n - 2; i >= 0; i--) {
            for (int j = 0; j <= i; j++) {
                dp[i][j] = triangle.get(i).get(j) + Math.min(dp[i + 1][j], dp[i + 1][j + 1]);
            }
        }

        return dp[0][0];
    }

    public static int minimumTotalOptimized(List<List<Integer>> triangle) {
        if (triangle == null || triangle.size() == 0) {
            return 0;
        }

        int n = triangle.size();
        int[] dp = new int[n + 1];

        for (int i = n - 1; i >= 0; i--) {
            List<Integer> currentRow = triangle.get(i);
            for (int j = 0; j < currentRow.size(); j++) {
                dp[j] = currentRow.get(j) + Math.min(dp[j], dp[j + 1]);
            }
        }

        return dp[0];
    }

    public static void main(String[] args) {
        List<List<Integer>> triangle = new ArrayList<>();
        triangle.add(Arrays.asList(2));
        triangle.add(Arrays.asList(3, 4));
        triangle.add(Arrays.asList(6, 5, 7));
        triangle.add(Arrays.asList(4, 1, 8, 3));

        System.out.println("示例 1 输入: " + triangle);
        System.out.println("标准解法输出: " + minimumTotal(triangle));
        System.out.println("优化解法输出: " + minimumTotalOptimized(triangle));
        System.out.println("-----------------------------");

        List<List<Integer>> triangle2 = new ArrayList<>();
        triangle2.add(Arrays.asList(-10));

        System.out.println("示例 2 输入: " + triangle2);
        System.out.println("标准解法输出: " + minimumTotal(triangle2));
        System.out.println("优化解法输出: " + minimumTotalOptimized(triangle2));
    }
}
```

