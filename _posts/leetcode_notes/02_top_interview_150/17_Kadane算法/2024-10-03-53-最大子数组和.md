---
title: 53. 最大子数组和
date: 2024-10-03 06:00:00 +0800
categories: [leetcode_notes, top_interview_150, kadane算法]
tags: [kadane算法]
toc: true
math: true
pin: false
render_with_liquid: false
image:
  path: https://cdn.jsdelivr.net/gh/Zong-Liang/blog_images/blog/2024/daily_coding/20251121234207752.png
  lqip: data:image/webp;base64,UklGRpoAAABXRUJQVlA4WAoAAAAQAAAADwAABwAAQUxQSDIAAAARL0AmbZurmr57yyIiqE8oiG0bejIYEQTgqiDA9vqnsUSI6H+oAERp2HZ65qP/VIAWAFZQOCBCAAAA8AEAnQEqEAAIAAVAfCWkAALp8sF8rgRgAP7o9FDvMCkMde9PK7euH5M1m6VWoDXf2FkP3BqV0ZYbO6NA/VFIAAAA
---

## 题目

**描述：**

给你一个整数数组 `nums`，请你找出一个具有最大和的连续子数组（子数组最少包含一个元素），返回其最大和。

**子数组** 是数组中的一个连续部分。**示例 1:**

- **输入:** `nums = [-2,1,-3,4,-1,2,1,-5,4]`
- **输出:** 6
- **解释:** 连续子数组 `[4,-1,2,1]` 的和最大，为 6。**示例 2:**

- **输入:** `nums = [1]`
- **输出:** 1**示例 3:**

- **输入:** `nums = [5,4,-1,7,8]`
- **输出:** 23**提示:**

- `1 <= nums.length <= 10^5`
- `-10^4 <= nums[i] <= 10^4`

**进阶：** 如果你已经实现复杂度为 `O(n)` 的解法，尝试使用更为精妙的**分治法** 求解。

## 题解


这题最经典的解法是 Kadane：遍历到 `x` 时，只关心“以当前位置结尾的最大子数组和”该不该把前一段接上。

代码里的 `pre = max(pre + x, x)` 就是这个决策：如果前缀和为负，就从当前元素重新开始；否则把它接上。再用 `maxAns` 持续维护全局最优即可。

文件里也给了分治版（`Status` 四元组）作为拓展，它通过合并左右区间状态来得到跨中点最优值，思路更偏模板化。面试主答通常 Kadane 就够了。

Kadane 的时间复杂度是 $O(n)$、空间复杂度是 $O(1)$；分治版时间复杂度是 $O(n\log n)$。

这题背后最值得形成习惯的是：当目标是“连续子数组最优”时，先尝试把状态压成“以 i 结尾”的一维转移。

完整代码如下：

```java
import java.util.Arrays;

public class Main {

    public static int maxSubArray(int[] nums) {
        int pre = 0;
        int maxAns = nums[0];

        for (int x : nums) {
            pre = Math.max(pre + x, x);
            maxAns = Math.max(maxAns, pre);
        }

        return maxAns;
    }

    static class Status {
        int lSum, rSum, mSum, iSum;

        public Status(int lSum, int rSum, int mSum, int iSum) {
            this.lSum = lSum;
            this.rSum = rSum;
            this.mSum = mSum;
            this.iSum = iSum;
        }
    }

    public static int maxSubArrayDivideConquer(int[] nums) {
        return getInfo(nums, 0, nums.length - 1).mSum;
    }

    private static Status getInfo(int[] nums, int left, int right) {
        if (left == right) {
            return new Status(nums[left], nums[left], nums[left], nums[left]);
        }

        int mid = left + (right - left) / 2;
        Status leftSub = getInfo(nums, left, mid);
        Status rightSub = getInfo(nums, mid + 1, right);

        return pushUp(leftSub, rightSub);
    }

    private static Status pushUp(Status left, Status right) {
        int iSum = left.iSum + right.iSum;
        int lSum = Math.max(left.lSum, left.iSum + right.lSum);
        int rSum = Math.max(right.rSum, right.iSum + left.rSum);
        int mSum = Math.max(Math.max(left.mSum, right.mSum), left.rSum + right.lSum);

        return new Status(lSum, rSum, mSum, iSum);
    }

    public static void main(String[] args) {
        int[] nums1 = {-2, 1, -3, 4, -1, 2, 1, -5, 4};
        System.out.println("示例 1 输入: " + Arrays.toString(nums1));
        System.out.println("Kadane 输出: " + maxSubArray(nums1));
        System.out.println("分治输出: " + maxSubArrayDivideConquer(nums1));
        System.out.println("-----------------------------");

        int[] nums2 = {1};
        System.out.println("示例 2 输入: " + Arrays.toString(nums2));
        System.out.println("Kadane 输出: " + maxSubArray(nums2));
        System.out.println("分治输出: " + maxSubArrayDivideConquer(nums2));
        System.out.println("-----------------------------");

        int[] nums3 = {5, 4, -1, 7, 8};
        System.out.println("示例 3 输入: " + Arrays.toString(nums3));
        System.out.println("Kadane 输出: " + maxSubArray(nums3));
        System.out.println("分治输出: " + maxSubArrayDivideConquer(nums3));
    }
}
```
