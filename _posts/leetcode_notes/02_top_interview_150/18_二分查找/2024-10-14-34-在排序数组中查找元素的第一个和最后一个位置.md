---
title: 34. 在排序数组中查找元素的第一个和最后一个位置
date: 2024-10-14 06:00:00 +0800
categories: [leetcode_notes, top_interview_150, 二分查找]
tags: [二分查找]
toc: true
math: true
pin: false
render_with_liquid: false
image:
  path: https://cdn.jsdelivr.net/gh/Zong-Liang/blog_images/blog/2024/daily_coding/20251121234541349.png
  lqip: data:image/webp;base64,UklGRpoAAABXRUJQVlA4WAoAAAAQAAAADwAABwAAQUxQSDIAAAARL0AmbZurmr57yyIiqE8oiG0bejIYEQTgqiDA9vqnsUSI6H+oAERp2HZ65qP/VIAWAFZQOCBCAAAA8AEAnQEqEAAIAAVAfCWkAALp8sF8rgRgAP7o9FDvMCkMde9PK7euH5M1m6VWoDXf2FkP3BqV0ZYbO6NA/VFIAAAA
---

## 题目

**描述：**

给你一个按照非递减顺序排列的整数数组 `nums`，和一个目标值 `target`。请你找出给定目标值在数组中的开始位置和结束位置。

如果数组中不存在目标值 `target`，返回 `[-1, -1]`。

你必须设计并实现时间复杂度为 `O(log n)` 的算法解决此问题。

**示例 1:**

- **输入:** `nums = [5,7,7,8,8,10]`, `target = 8`
- **输出:** `[3,4]`**示例 2:**

- **输入:** `nums = [5,7,7,8,8,10]`, `target = 6`
- **输出:** `[-1,-1]`**示例 3:**

- **输入:** `nums = []`, `target = 0`
- **输出:** `[-1,-1]`**提示:**

- `0 <= nums.length <= 10^5`
- `-10^9 <= nums[i] <= 10^9`
- `nums` 是一个非递减数组
- `-10^9 <= target <= 10^9`

## 题解
这题不能用“找到就返回”的普通二分，因为题目要的是左右边界。正确思路是做两次二分：一次找最左出现位置，一次找最右出现位置。

找左边界时，`nums[mid] == target` 也不能停，要先记录 `mid`，再继续向左收缩；找右边界时同理，记录后继续向右收缩。这样最后留下的才是极值位置，而不是任意一个命中点。

把它理解成“二分找边界模板”会更稳：命中时不返回，只移动边界并保留暂存答案。两次模板拼起来就是结果区间。

代码如下：

```java
import java.util.Arrays;

public class Main {

    public static int[] searchRange(int[] nums, int target) {
        int first = findBound(nums, target, true);
        int last = findBound(nums, target, false);
        return new int[]{first, last};
    }

    private static int findBound(int[] nums, int target, boolean isFirst) {
        int left = 0;
        int right = nums.length - 1;
        int res = -1;

        while (left <= right) {
            int mid = left + (right - left) / 2;

            if (nums[mid] == target) {
                res = mid;
                if (isFirst) {
                    right = mid - 1;
                } else {
                    left = mid + 1;
                }
            } else if (nums[mid] < target) {
                left = mid + 1;
            } else {
                right = mid - 1;
            }
        }

        return res;
    }

    public static void main(String[] args) {
        int[] nums1 = {5, 7, 7, 8, 8, 10};
        int target1 = 8;
        System.out.println("示例 1 输入: nums = " + Arrays.toString(nums1) + ", target = " + target1);
        System.out.println("示例 1 输出: " + Arrays.toString(searchRange(nums1, target1)));
        System.out.println("-----------------------------");

        int[] nums2 = {5, 7, 7, 8, 8, 10};
        int target2 = 6;
        System.out.println("示例 2 输入: nums = " + Arrays.toString(nums2) + ", target = " + target2);
        System.out.println("示例 2 输出: " + Arrays.toString(searchRange(nums2, target2)));
        System.out.println("-----------------------------");

        int[] nums3 = {};
        int target3 = 0;
        System.out.println("示例 3 输入: nums = " + Arrays.toString(nums3) + ", target = " + target3);
        System.out.println("示例 3 输出: " + Arrays.toString(searchRange(nums3, target3)));
        System.out.println("-----------------------------");

        int[] nums4 = {1};
        int target4 = 1;
        System.out.println("额外示例 输入: nums = " + Arrays.toString(nums4) + ", target = " + target4);
        System.out.println("额外示例 输出: " + Arrays.toString(searchRange(nums4, target4)));
    }
}
```

复杂度分析：时间复杂度 `O(log n)`（两次二分仍是同阶），空间复杂度 `O(1)`。

很多区间题都能复用这个写法，尤其是“第一个/最后一个满足条件的位置”。
