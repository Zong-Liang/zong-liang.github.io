---
title: 295. 数据流的中位数
date: 2024-10-26 06:00:00 +0800
categories: [leetcode_notes, top_interview_150, 堆]
tags: [堆]
toc: true
math: true
pin: false
render_with_liquid: false
image:
  path: https://cdn.jsdelivr.net/gh/Zong-Liang/blog_images/blog/2024/daily_coding/20251121234843031.png
  lqip: data:image/webp;base64,UklGRpoAAABXRUJQVlA4WAoAAAAQAAAADwAABwAAQUxQSDIAAAARL0AmbZurmr57yyIiqE8oiG0bejIYEQTgqiDA9vqnsUSI6H+oAERp2HZ65qP/VIAWAFZQOCBCAAAA8AEAnQEqEAAIAAVAfCWkAALp8sF8rgRgAP7o9FDvMCkMde9PK7euH5M1m6VWoDXf2FkP3BqV0ZYbO6NA/VFIAAAA
---

## 题目

**描述：**

**中位数**是有序整数列表中的中间值。如果列表的大小是偶数，则没有中间值，中位数是两个中间值的平均值。

- 例如 `arr = [2,3,4]` 的中位数是 `3`。
- 例如 `arr = [2,3]` 的中位数是 `(2 + 3) / 2 = 2.5`。

实现 `MedianFinder` 类：

- `MedianFinder()` 初始化 `MedianFinder` 对象。
- `void addNum(int num)` 将数据流中的整数 `num` 添加到数据结构中。
- `double findMedian()` 返回到目前为止所有元素的中位数。与实际答案相差 `10^-5` 以内的答案将被接受。

**示例 1:**

- **输入**

  `["MedianFinder", "addNum", "addNum", "findMedian", "addNum", "findMedian"]`

  `[[], [1], [2], [], [3], []]`

- **输出**

  `[null, null, null, 1.5, null, 2.0]`

- **解释**

  ```
  MedianFinder medianFinder = new MedianFinder();
  medianFinder.addNum(1);    // arr = [1]
  medianFinder.addNum(2);    // arr = [1, 2]
  medianFinder.findMedian(); // 返回 1.5 ((1 + 2) / 2)
  medianFinder.addNum(3);    // arr = [1, 2, 3]
  medianFinder.findMedian(); // 返回 2.0
  ```

**提示:**

- `-10^5 <= num <= 10^5`
- 在调用 `findMedian` 之前，数据结构中至少有一个元素
- 最多 `5 * 10^4` 次调用 `addNum` 和 `findMedian`

## 题解


数据流场景下不能每次都全量排序，常用解法是双堆：一个大顶堆存较小一半，一个小顶堆存较大一半。

这份代码的平衡策略很稳：新数先进大顶堆，再把大顶堆堆顶挪到小顶堆；如果小顶堆元素变多，再挪回一个。这样能保持两个不变量：`smallHalf` 的元素都不大于 `largeHalf`，且 `smallHalf` 的数量始终等于或只比 `largeHalf` 多 1。

查询中位数时就很直接：若两堆大小不同，取大顶堆堆顶；若相同，取两堆堆顶均值。

`addNum` 的时间复杂度是 $O(\log n)$，`findMedian` 是 $O(1)$，空间复杂度是 $O(n)$。

这题后面经常复用在“动态第 k 大/第 k 小”问题里，本质都是用堆维持一个实时平衡分界。

完整代码如下：

```java
import java.util.PriorityQueue;

public class Main {

    static class MedianFinder {
        private PriorityQueue<Integer> smallHalf;
        private PriorityQueue<Integer> largeHalf;

        public MedianFinder() {
            smallHalf = new PriorityQueue<>((a, b) -> b - a);
            largeHalf = new PriorityQueue<>();
        }

        public void addNum(int num) {
            smallHalf.offer(num);
            largeHalf.offer(smallHalf.poll());

            if (smallHalf.size() < largeHalf.size()) {
                smallHalf.offer(largeHalf.poll());
            }
        }

        public double findMedian() {
            if (smallHalf.size() > largeHalf.size()) {
                return smallHalf.peek();
            }
            return (smallHalf.peek() + largeHalf.peek()) / 2.0;
        }
    }

    public static void main(String[] args) {
        MedianFinder medianFinder = new MedianFinder();

        medianFinder.addNum(1);
        System.out.println("addNum(1)");

        medianFinder.addNum(2);
        System.out.println("addNum(2)");

        System.out.println("findMedian() -> " + medianFinder.findMedian());

        medianFinder.addNum(3);
        System.out.println("addNum(3)");

        System.out.println("findMedian() -> " + medianFinder.findMedian());

        System.out.println("-----------------------------");

        medianFinder.addNum(4);
        System.out.println("addNum(4), findMedian() -> " + medianFinder.findMedian());

        medianFinder.addNum(5);
        System.out.println("addNum(5), findMedian() -> " + medianFinder.findMedian());
    }
}
```
